# NDDev Codex Safe Setup

Work within the user's stated scope and repository instructions. Treat the
environment as read-only unless the user deliberately selects a more capable
permission profile. Report blocked mutations precisely; never weaken the
permission model or bypass approval boundaries.
